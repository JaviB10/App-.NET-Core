@model IEnumerable<Turnos.Models.Especialidad>

<!DOCTYPE html>

<table>
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Descripcion)</th>
        </tr>
    </thead>
    <tbody>
    @*Aqui colocaremos mas adelante el codigo para mostrar los registros de la base de datos*@
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.EspecialidadID">Editar</a>
                    <a asp-action="Delete" asp-route-id="@item.EspecialidadID">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
  